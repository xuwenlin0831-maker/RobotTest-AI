# CHANGELOG

## v0.2.0 — 2026-09-20

- 将 Skill 放入仓库级 `.agents/skills` 目录，并补充 Codex 项目式安装、ChatGPT 网页兼容说明、FAQ 与对话示例；本项不改变 TB 输出规则。
- 保持 TB 17 列模板不变，明确不得自行增加“用例状态”列。
- 明确“用例状态”不属于 17 列模板：不写状态也可导入，Skill 不增加状态列或校验状态。
- 将“编写人”设为正式生成硬门槛：整批默认同一人，缺失时停止生成并集中询问。
- 非必填字段允许留空并进入评审清单；导入关键字段不得使用“待确认”占位。
- 增加两级确认：信息完整且已明确要求生成时可输出本地文件；任何上传、发送或外部系统写入必须再次获得明确授权。
- 状态仍为“规划中-待试用”，等待真实 TB 非生产试导入验证模板输出。

## v0.1.0 — 2026-09-05

- 将标准 Skill 定义为 `vehicle-testcase-lifecycle`。
- 确认 TB 17 列模板为第一阶段唯一输出标准。
- 将“派生用例输出”设为默认模式。
- 增加基础库治理建议、双层版本和 TB 规则变更的管理边界。
- 状态为“规划中-待试用”，尚未用真实需求完成验证。
